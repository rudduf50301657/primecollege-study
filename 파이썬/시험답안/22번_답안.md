# 파이썬 시험 22번 답안

## 문제

리스트 슬라이싱에 대한 설명으로 옳은 것은?

## 답: ③

**리스트의 일부 구간을 잘라 새로운 부분 리스트를 만들 수 있다.**

## 풀이

```python
a = [1, 2, 3, 4, 5]
a[1:3]  # [2, 3] — 인덱스 1~2 구간의 새 리스트
```

- ① 슬라이싱 후 원본이 삭제된다 → 원본은 그대로 유지됨
- ② 세트에서도 사용 가능 → 세트는 순서가 없어 슬라이싱 불가
- ④ 인덱스를 사용할 수 없다 → 인덱스를 사용하여 범위 지정
