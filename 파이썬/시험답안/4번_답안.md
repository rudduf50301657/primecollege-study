# 파이썬 시험 4번 답안

## 문제

클래스의 __init__ 메소드에 대한 설명으로 옳은 것은?

## 답: ①

**객체가 생성될 때 자동으로 호출되어 초기화를 담당한다.**

## 풀이

`__init__`은 생성자(constructor)로, 객체가 만들어질 때 자동 호출되어 속성을 초기화한다.

- ② 객체를 메모리에서 삭제할 때 호출 → `__del__` 메소드
- ③ 객체의 값을 화면에 출력 → `__str__` 메소드
- ④ 클래스 이름을 바꾸는 메소드 → 해당 없음
