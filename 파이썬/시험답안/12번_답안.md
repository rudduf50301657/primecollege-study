# 파이썬 시험 12번 답안

## 문제

f-문자열에 대한 설명으로 옳은 것은?

## 답: ③

**문자열 앞에 f를 붙이고 {} 안에 변수나 표현식을 넣어 값을 출력할 수 있다.**

## 풀이

```python
name = "홍길동"
print(f"이름: {name}")  # 이름: 홍길동
```

- ① 숫자만 출력 가능 → 문자열, 변수, 표현식 모두 가능
- ② 모든 문자에 'f'가 추가 → 틀림
- ④ 주석 작성용 → 주석은 # 사용
