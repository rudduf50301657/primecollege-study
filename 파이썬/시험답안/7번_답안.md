# 파이썬 시험 7번 답안

## 문제

다음 코드의 실행 결과로 옳은 것은?

```python
msg = "I do love python"
msg_list = msg.split(" ")
print(msg_list)
```

## 답: ①

**["I", "do", "love", "python"]**

## 풀이

`split(" ")`은 문자열을 공백(" ") 기준으로 나누어 리스트로 반환한다.

"I do love python" → 공백 기준 분리 → ["I", "do", "love", "python"]
