# 파이썬 시험 21번 답안

## 문제

모듈에 대한 설명으로 가장 적절한 것은?

## 답: ②

**함수, 상수, 클래스 등을 모아 하나의 단위로 재사용할 수 있게 만든 것이다.**

## 풀이

모듈(module)은 관련 함수, 변수, 클래스를 하나의 .py 파일에 모아 재사용 가능하게 만든 것이다.

- ① 순서 있게 저장하는 불변 시퀀스 → 튜플(tuple)
- ③ key-value 쌍으로 저장하는 자료형 → 딕셔너리(dict)
- ④ 사용자 입력을 문자열로 반환하는 함수 → input()
