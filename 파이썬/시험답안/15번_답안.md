# 파이썬 시험 15번 답안

## 문제

다음 중 함수 정의 형식으로 가장 적절한 것은?

## 답: ①

**def 함수이름(파라미터 리스트):**

## 풀이

파이썬에서 함수 정의는 `def` 키워드를 사용한다.

```python
def 함수이름(파라미터):
    실행할 코드
```

- ② function → JavaScript 문법
- ③ if 함수이름 = 파라미터: → 조건문 문법
- ④ return 함수이름; → 반환문이지 정의가 아님
