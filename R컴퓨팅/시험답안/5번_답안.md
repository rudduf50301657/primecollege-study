# R컴퓨팅 시험 5번 답안
## 학번: 202657-102185

---

## 문제 (배점 5점)

함수 f()에 대하여 파이프 연산자(%>%)를 활용한 다음 명령문과 동일한 기능을 수행하는 것은?

```r
x %>% f(y)
```

---

## 답: ③ f(x, y)

---

## 풀이

파이프 연산자 `%>%`는 왼쪽의 값을 오른쪽 함수의 **첫 번째 인자**로 전달한다.

따라서 `x %>% f(y)`는 x를 f()의 첫 번째 인자로 넣고, y를 두 번째 인자로 넣는 것이므로 `f(x, y)`와 동일하다.
